<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE concept
  PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept xmlns:ditaarch="http://dita.oasis-open.org/architecture/2005/" id="topic_11_1">
   <title id="title_a44">Checking Space for the Data Store</title>
   <conbody>
      <p>To determine the space and inode (i-number) usage for data store, use the
        <codeph>ds_stat</codeph> utility. Run the <codeph>ds_stat</codeph> command to make estimates
      and projections of disk space usage on the EDW.</p>
      <p>
         <codeph>ds_stat { &lt;<i>file</i>&gt; | &lt;<i>directory</i>&gt; }</codeph>
      </p>
      <p>The <codeph>ds_stat</codeph> utility takes a single parameter, which is the name of a
      directory or the name of a file. If the argument is a file, the utility generates disk usage
      statistics for that file. If the argument is a directory, utility generates a summary of the
      disk usage statistics for the entire directory tree.</p>
      <p>The statistics that <codeph>ds_stat</codeph> generates are:</p>
      <ul>
         <li>Total inodes used</li>
         <li>Total file size</li>
         <li>Total disk blocks used</li>
         <li>Total disk space used</li>
         <li>Ration of disk space to inodes</li>
      </ul>
   </conbody>
</concept>